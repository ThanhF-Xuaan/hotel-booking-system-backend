spring:
  application:
    name: hotel-booking-system
  profiles:
    active: dev

server:
  port: 8080

jwt:
  signerKey: ${JWT_SECRET:8e18c9e3f2cab32b039df13a3c1ee6def879acad674f279daba9066d6dd62e5a}
  expiration: ${JWT_EXPIRATION_MS:86400000}

# CẤU HÌNH LOGGING ĐỂ DEBUG
logging:
  level:
    root: INFO
    # Log chi tiết quá trình xác thực của Spring Security
    org.springframework: INFO

    # Log chi tiết các class của bạn (Thay 'com.hotel.booking' bằng package của bạn)
    com.hotel.booking: DEBUG